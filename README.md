# 🔐 Captcha Generator (HTML + CSS + JavaScript)

This project is a **client-side Captcha Generator** built using **pure HTML, CSS, and JavaScript** — no frameworks required.  
It generates a distorted captcha using `<canvas>`, adds noise and random transformations, and includes an optional audio accessibility mode using the browser's SpeechSynthesis API.

---

## 🚀 Features

✅ Generates random CAPTCHA codes (letters + numbers)  
✅ Canvas rendering with distortion + noise for security  
✅ Customizable code length (4–6 characters)  
✅ Audio playback for accessibility (text-to-speech)  
✅ Responsive and modern UI design  
✅ Clean, maintainable, and modular JS  
✅ No external libraries needed

---

## 🧩 Tech Stack

**HTML5**: Structure and layout
**CSS3**: Styling, layout responsiveness, modern UI
**JavaScript (ES6)**: CAPTCHA logic, canvas drawing, audio accessibility

---

## 📂 Project Structure

├── index.html # Main markup
├── script.js # Captcha logic and canvas rendering
└── styles.css # Styles and layout

---

## 🚀 How to Use

1. **Clone this repository** or download the project files.

   ```bash
   git clone https://github.com/matifelitti/Captcha-Generator.git
   ```

2. **Open `index.html`** in your browser.
